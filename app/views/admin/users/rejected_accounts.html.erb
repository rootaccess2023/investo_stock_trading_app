<div class="flex">
    <section>
        <%= render "shared/nav"%>
    </section>
  <main class="w-full flex flex-col p-8 bg-teal-50 overflow-scroll">
    <%= render "shared/admin_search"%>
    <table class="w-full text-sm text-left rtl:text-right text-gray-500 dark:text-gray-400">
        <% if @rejected_traders.empty? %>
         <caption class="p-5 text-lg font-semibold text-left rtl:text-right text-gray-900">
            No Rejected Accounts
            <p class="mt-1 text-sm font-normal text-gray-500 text-justify dark:text-gray-400">
                Rejected accounts are profiles within our stock trading app that did not meet the necessary criteria or requirements during the account setup or verification process. We strive to ensure a secure and compliant trading environment, and part of this involves verifying all account applications thoroughly.
            </p>
        </caption>
        <% else %>
        <caption class="p-5 text-lg font-semibold text-left rtl:text-right text-gray-900">
            Rejected Accounts
            <p class="mt-1 text-sm font-normal text-gray-500 text-justify dark:text-gray-400">
                Rejected accounts are profiles within our stock trading app that did not meet the necessary criteria or requirements during the account setup or verification process. We strive to ensure a secure and compliant trading environment, and part of this involves verifying all account applications thoroughly.
            </p>
        </caption>
        <thead class="text-xsuppercase bg-teal-700 text-white">
            <tr>
                <th scope="col" class="px-6 py-3">
                    Email
                </th>
                <th scope="col" class="px-6 py-3">
                    Action
                </th>
            </tr>
        </thead>
        <tbody>
          <% @rejected_traders.each do |user| %>
            <tr class="bg-teal-50 border-b border-gray-700">
                <th scope="row" class="px-6 py-4 font-medium text-black whitespace-nowrap">
                    <%= user.email %>
                </th>
                <td class="flex px-6 py-4 text-black gap-4">
                    <%= link_to add_to_pending_admin_user_path(user), data: { "turbo-method": :patch } do %>
                        <p class="w-fit text-white bg-blue-600 font-medium px-2 py-1 rounded">Rescind</p>
                    <% end %>
                    <%= link_to delete_account_admin_user_path(user), data: { "turbo-method": :patch } do %>
                        <p class="w-fit text-white bg-red-600 font-medium px-2 py-1 rounded"><%= fa_icon "trash" %></p>
                    <% end %>
                </td>
            </tr>
          <% end %>
        </tbody>
        <% end %>
    </table>
</div>
</main>
</div>